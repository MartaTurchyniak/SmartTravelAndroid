<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/main_pink</item>
        <item name="colorPrimaryDark">@android:color/white</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="ThemeOverlay.MyTextDetail" parent="AppTheme">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:fadeScrollbars">true</item>
        <item name="android:gravity">center</item>
        <item name="android:maxLines">5</item>
        <item name="android:padding">4dp</item>
        <item name="android:scrollbars">vertical</item>
        <item name="android:textSize">14sp</item>
    </style>

    <style name="ThemeOverlay.FirebaseIcon" parent="AppTheme">
        <item name="android:layout_marginTop">@dimen/icon_top_margin</item>
        <item name="android:layout_marginBottom">@dimen/icon_bottom_margin</item>
    </style>

    <style name="ThemeOverlay.MyTitleText" parent="AppTheme">
        <item name="android:gravity">center</item>
        <item name="android:textSize">36sp</item>
    </style>
    `
    <!-- Dark Buttons -->
    <style name="ThemeOverlay.MyDarkButton" parent="ThemeOverlay.AppCompat.Dark">
        <item name="colorButtonNormal">@color/colorAccent</item>
        <item name="android:layout_marginRight">4dp</item>
        <item name="android:layout_marginLeft">4dp</item>
        <item name="android:textColor">@android:color/white</item>
    </style>

    <style name="MyEditTextTheme">
        <item name="colorControlNormal">@color/grey_300</item>
        <item name="colorControlActivated">#FDAFBB</item>
        <item name="colorControlHighlight">#FDAFBB</item>
    </style>

    <style name="BottomNavigationTheme" parent="Widget.Design.BottomNavigationView">
        <item name="android:textColorSecondary">@color/main_pink</item>
        <item name="android:colorForeground">@color/grey_500</item>
    </style>

    <style name="ChatIncomingLabel">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textColor">@color/main_gray_dark</item>
        <item name="android:textSize">16sp</item>
    </style>

    <style name="ChatIncomingLabelSender">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textColor">@color/main_gray_dark</item>
        <item name="android:textSize">14sp</item>
        <item name="android:textStyle">bold</item>
    </style>

    <style name="ChatIncomingMessage">
        <item name="android:background">@drawable/destination_bg</item>
        <item name="android:paddingStart">@dimen/s16</item>
        <item name="android:paddingEnd">@dimen/s16</item>
        <item name="android:paddingTop">@dimen/s12</item>
        <item name="android:paddingBottom">@dimen/s12</item>
    </style>

    <style name="ChatOutgoingLabel">
        <item name="android:textSize">16sp</item>
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textColor">@android:color/white</item>
        <item name="android:textAlignment">textEnd</item>
    </style>

    <style name="ChatOutgoingMessage">
        <item name="android:background">@drawable/chat_bg</item>
        <item name="android:paddingStart">@dimen/s16</item>
        <item name="android:paddingEnd">@dimen/s16</item>
        <item name="android:paddingTop">@dimen/s12</item>
        <item name="android:paddingBottom">@dimen/s12</item>
    </style>

    <style name="ChatOutgoingLabelSender">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textColor">@android:color/white</item>
        <item name="android:textSize">14sp</item>
        <item name="android:textStyle">bold</item>
    </style>

    <dimen name="activity_horizontal_margin">16dp</dimen>
    <dimen name="activity_vertical_margin">16dp
    </dimen>
    <dimen name="icon_top_margin">24dp</dimen>
    <dimen name="icon_bottom_margin">24dp
    </dimen>
    <dimen name="title_bottom_margin">16dp</dimen>
    <dimen name="field_width_standard">160dp
    </dimen>

    <dimen name="button_horizontal_margin">8dp</dimen>

</resources>
